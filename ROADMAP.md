# 🗺️ ДОРОЖНАЯ КАРТА РАЗВИТИЯ ПРОЕКТА CHILDWATCH

**Документ обновлен:** 26 октября 2025, 19:00
**Текущие версии:** ChildWatch v5.5.0 (31) | ParentWatch v6.3.0 (25)
**Статус сборки:** ✅ BUILD SUCCESSFUL

---

## 📊 ТЕКУЩЕЕ СОСТОЯНИЕ ПРОЕКТА

### ✅ Реализованные функции (v5.5.0 / v6.3.0)

#### 1. Система мониторинга
- ✅ Геолокация с настраиваемым интервалом (10-300 сек)
- ✅ Аудио прослушка в реальном времени через WebSocket
- ✅ Запись аудио потока в файл
- ✅ Фоновая служба мониторинга
- ✅ Система диагностики (HUD) с метриками

#### 2. Аудио возможности
- ✅ 5 режимов аудио фильтров (Оригинал, Голос, Музыка, Природа, Тишина)
- ✅ 4 режима усиления громкости (Авто, x2, x4, x8)
- ✅ 2 типа визуализации (Эквалайзер, Осциллограф)
- ✅ Библиотека записей с фильтрацией
- ✅ Адаптивная буферизация и Quality of Service

#### 3. Чат (v5.5.0)
- ✅ Двусторонний обмен сообщениями
- ✅ Статусы сообщений (Отправка, Доставлено, Прочитано)
- ✅ Цветные индикаторы статусов (✓ серая, ✓✓ серая, ✓✓ зеленая)
- ✅ Современный дизайн с градиентами
- ✅ Emoji picker (40 эмодзи)
- ✅ Локальное хранилище сообщений (JSON)
- ✅ Лимит истории: 100 сообщений

#### 4. Уведомления (v6.3.0)
- ✅ MessagingStyle с историей (последние 10 сообщений)
- ✅ Настройка звука и вибрации
- ✅ Быстрый ответ (Quick Reply) из уведомления
- ✅ Приоритеты уведомлений (Низкий/Средний/Высокий)
- ✅ Счетчик непрочитанных сообщений
- ✅ Видимость на экране блокировки

#### 5. Система профилей пользователей (v5.5.0) 🆕
- ✅ SQLite/Room база данных для хранения данных
- ✅ Профили детей (Child) с именем, возрастом, телефоном, фото
- ✅ Привязка данных к Device ID
- ✅ Выбор активного ребенка из списка
- ✅ Добавление/редактирование/удаление профилей детей
- ✅ Автозаполнение из контактов телефона
- ✅ QR-код сканер для Device ID
- ✅ Генерация QR-кода в ParentWatch

#### 6. UI/UX
- ✅ Material Design 3
- ✅ Карточный интерфейс главного меню
- ✅ Адаптивные темы (светлая/темная)
- ✅ Система согласия (Consent Screen)
- ✅ QR-код для сопряжения устройств
- ✅ Настройки с валидацией
- ✅ Улучшенный диалог добавления/редактирования детей
- ✅ Выбор фото из галереи с постоянным доступом к URI
- ✅ Обработка SecurityException для недоступных аватаров

#### 7. Безопасность
- ✅ Шифрование данных (SecurePreferences)
- ✅ Парольная защита настроек
- ✅ Проверка root/mock location
- ✅ Обфускация кода (ProGuard)
- ✅ Отключение автозаполнения для Device ID полей

---

## 🚀 УТВЕРЖДЕННЫЙ ПЛАН РАБОТ (26 октября 2025)

### 📅 **НЕДЕЛЯ 1: Исправление критических проблем чата**

---

## ФАЗА 1: ПЕРСОНАЛИЗАЦИЯ ЧАТА (v5.6.0 / v6.4.0)
**Приоритет:** 🔴 КРИТИЧЕСКИЙ
**Срок:** Понедельник-Вторник (27-28 октября)
**Трудозатраты:** 4-6 часов
**Статус:** ⏳ ГОТОВО К НАЧАЛУ
**Ответственный:** Любой разработчик может продолжить по этому плану

### 📋 Проблема:
Сейчас в чате показываются захардкоженные имена "Родитель" и "Ребенок", хотя профили уже созданы в БД. Это снижает персонализацию и UX.

### 🎯 Цель:
Показывать реальные имена из профилей пользователей в чате.

---

### 📝 Детальный план задач:

#### **ДЕНЬ 1: ChildWatch (Родительское приложение)**

**Задача 1.1: Обновление ChatActivity.kt** (1.5 часа)

**Файл:** `app/src/main/java/ru/example/childwatch/ChatActivity.kt`

**Что сделать:**
```kotlin
// 1. В onCreate() загрузить выбранного ребенка из БД
private lateinit var selectedChild: Child
private var parentName: String = "Вы"

override fun onCreate(savedInstanceState: Bundle?) {
    super.onCreate(savedInstanceState)

    // Загрузить выбранного ребенка
    lifecycleScope.launch {
        val prefs = getSharedPreferences("childwatch_prefs", MODE_PRIVATE)
        val deviceId = prefs.getString("selected_device_id", null)

        if (deviceId != null) {
            val database = ChildWatchDatabase.getInstance(this@ChatActivity)
            val childRepository = ChildRepository(database.childDao())

            selectedChild = childRepository.getChildByDeviceId(deviceId)
                ?: Child(deviceId = deviceId, name = "Ребенок") // fallback

            // Обновить заголовок
            supportActionBar?.title = "Чат с ${selectedChild.name}"

            // Передать имена в адаптер
            chatAdapter.updateNames(
                parentName = parentName,
                childName = selectedChild.name
            )
        }
    }
}
```

**Что изменить:**
1. Добавить поля `selectedChild` и `parentName`
2. Загружать ребенка по `selected_device_id` из БД
3. Обновлять заголовок Activity с именем ребенка
4. Передавать имена в ChatAdapter

---

**Задача 1.2: Обновление ChatAdapter.kt** (1 час)

**Файл:** `app/src/main/java/ru/example/childwatch/adapter/ChatAdapter.kt`

**Что сделать:**
```kotlin
class ChatAdapter(
    private val messages: List<ChatMessage>,
    private var parentName: String = "Родитель",  // добавить параметр
    private var childName: String = "Ребенок"     // добавить параметр
) : RecyclerView.Adapter<ChatAdapter.MessageViewHolder>() {

    // Добавить метод для обновления имен
    fun updateNames(parentName: String, childName: String) {
        this.parentName = parentName
        this.childName = childName
        notifyDataSetChanged()
    }

    override fun onBindViewHolder(holder: MessageViewHolder, position: Int) {
        val message = messages[position]

        // Использовать реальные имена вместо хардкода
        val senderName = if (message.senderType == "parent") {
            parentName
        } else {
            childName
        }

        // Остальной код...
    }
}
```

**Что изменить:**
1. Добавить параметры `parentName` и `childName` в конструктор
2. Создать метод `updateNames()` для обновления имен
3. Убрать все хардкод "Родитель"/"Ребенок"
4. Использовать переданные имена при отображении сообщений

---

#### **ДЕНЬ 2: ParentWatch (Детское приложение)**

**Задача 1.3: Обновление ChatActivity.kt (ParentWatch)** (1.5 часа)

**Файл:** `parentwatch/src/main/java/ru/example/parentwatch/ChatActivity.kt`

**Что сделать:**
```kotlin
// 1. Загрузить имя устройства/ребенка из настроек
private var childName: String = "Ребенок"
private var parentName: String = "Родитель"

override fun onCreate(savedInstanceState: Bundle?) {
    super.onCreate(savedInstanceState)

    // Загрузить имена из SharedPreferences
    val prefs = getSharedPreferences("parentwatch_prefs", MODE_PRIVATE)
    childName = prefs.getString("child_name", "Ребенок") ?: "Ребенок"
    // parentName можно получать от сервера или из сообщений

    // Обновить заголовок
    supportActionBar?.title = "Чат с родителем"

    // Передать имена в адаптер
    chatAdapter.updateNames(
        parentName = parentName,
        childName = childName
    )
}
```

**Что изменить:**
1. Загружать `child_name` из SharedPreferences
2. Обновлять заголовок Activity
3. Передавать имена в ChatAdapter
4. (Опционально) Добавить экран настройки имени в ParentWatch

---

**Задача 1.4: Обновление ChatAdapter.kt (ParentWatch)** (1 час)

**Файл:** `parentwatch/src/main/java/ru/example/parentwatch/chat/ChatAdapter.kt`

**Аналогично ChildWatch:**
1. Добавить параметры `parentName` и `childName`
2. Создать метод `updateNames()`
3. Убрать хардкод имен
4. Использовать реальные имена

---

**Задача 1.5: Тестирование** (30 минут)

**Что проверить:**
- [ ] Открыть чат в ChildWatch → заголовок показывает имя ребенка
- [ ] Сообщения в ChildWatch показывают реальные имена
- [ ] Открыть чат в ParentWatch → имена корректны
- [ ] Сообщения в ParentWatch показывают реальные имена
- [ ] Добавить второго ребенка → проверить, что имена не путаются
- [ ] Отправить сообщения в обе стороны → всё работает

---

### ✅ Критерии готовности ФАЗЫ 1:
- [x] Система профилей работает ✅ (уже готово)
- [ ] ChatActivity загружает профиль из БД
- [ ] Заголовок чата показывает имя ребенка
- [ ] ChatAdapter принимает и использует реальные имена
- [ ] В ChildWatch показываются имена из БД
- [ ] В ParentWatch показываются имена из настроек
- [ ] Тестирование пройдено
- [ ] Сборка успешна
- [ ] Все существующие функции работают

---

## ФАЗА 3: НАДЕЖНАЯ ДОСТАВКА СООБЩЕНИЙ (v5.7.0 / v6.5.0)
**Приоритет:** 🔴 КРИТИЧЕСКИЙ
**Срок:** Среда-Пятница (29-31 октября)
**Трудозатраты:** 8-12 часов
**Статус:** ⏳ ПОСЛЕ ФАЗЫ 1
**Ответственный:** Любой разработчик может продолжить по этому плану

### 📋 Проблема (КРИТИЧНО):
> **Из отзыва тестирования:** "доставка сообщений работает но не сразу и пока не работает то сообщения не доставляются и позже когда чат заработает **(сообщения теряются совсем)**. через какое то время (несколько минут или какой то активности в приложении) чат начинает работать в обе стороны."

**Это недопустимо** - теряющиеся сообщения делают приложение непригодным для использования!

### 🎯 Цель:
Обеспечить 100% доставку сообщений даже при нестабильном интернете.

---

### 📝 Детальный план задач:

#### **ДЕНЬ 1 (Среда): Диагностика проблемы** (2-3 часа)

**Задача 3.1: Расширенное логирование**

**Файлы для изменения:**
- `app/src/main/java/ru/example/childwatch/network/WebSocketManager.kt`
- `parentwatch/src/main/java/ru/example/parentwatch/network/WebSocketManager.kt`

**Что добавить:**
```kotlin
companion object {
    private const val TAG = "WebSocketManager"
}

// В методе connect()
Log.d(TAG, "Connecting to WebSocket: $url")

// В onOpen()
Log.d(TAG, "✅ WebSocket CONNECTED")

// В onMessage()
Log.d(TAG, "📥 Message RECEIVED: $text")

// В send()
Log.d(TAG, "📤 Sending message: $message")

// В onClosing()/onClosed()
Log.w(TAG, "⚠️ WebSocket CLOSING: code=$code, reason=$reason")

// В onFailure()
Log.e(TAG, "❌ WebSocket FAILED: ${t.message}", t)
```

**Что сделать:**
1. Добавить логи во все методы WebSocket
2. Логировать состояние соединения
3. Логировать каждое отправленное/полученное сообщение
4. Воспроизвести проблему и собрать логи
5. Определить root cause:
   - WebSocket отключается?
   - Сообщения не отправляются?
   - Проблема на сервере?
   - Network timeout?

---

#### **ДЕНЬ 2 (Четверг): Message Queue** (3-4 часа)

**Задача 3.2: Создание системы очереди сообщений**

**Новый файл:** `app/src/main/java/ru/example/childwatch/chat/MessageQueue.kt`

**Что создать:**
```kotlin
/**
 * Очередь неотправленных сообщений
 * Сохраняет сообщения при отсутствии связи и отправляет при восстановлении
 */
class MessageQueue(private val context: Context) {

    private val prefs = context.getSharedPreferences("message_queue", Context.MODE_PRIVATE)

    /**
     * Добавить сообщение в очередь
     */
    fun enqueue(message: ChatMessage) {
        val queue = getQueue().toMutableList()
        queue.add(message)
        saveQueue(queue)
        Log.d("MessageQueue", "Message queued: ${message.text}")
    }

    /**
     * Получить все сообщения из очереди
     */
    fun dequeueAll(): List<ChatMessage> {
        val queue = getQueue()
        clearQueue()
        Log.d("MessageQueue", "Dequeued ${queue.size} messages")
        return queue
    }

    /**
     * Проверить, есть ли сообщения в очереди
     */
    fun hasMessages(): Boolean {
        return getQueue().isNotEmpty()
    }

    private fun getQueue(): List<ChatMessage> {
        val json = prefs.getString("queue", "[]") ?: "[]"
        return Gson().fromJson(json, Array<ChatMessage>::class.java).toList()
    }

    private fun saveQueue(queue: List<ChatMessage>) {
        val json = Gson().toJson(queue)
        prefs.edit().putString("queue", json).apply()
    }

    private fun clearQueue() {
        prefs.edit().remove("queue").apply()
    }
}
```

**Что изменить в ChatActivity:**
```kotlin
private lateinit var messageQueue: MessageQueue

override fun onCreate(savedInstanceState: Bundle?) {
    // ...
    messageQueue = MessageQueue(this)

    // Отправить сообщения из очереди при подключении
    if (isConnected) {
        sendQueuedMessages()
    }
}

private fun sendMessage(text: String) {
    val message = ChatMessage(/* ... */)

    if (isConnected) {
        // Отправить немедленно
        webSocketManager.send(message)
    } else {
        // Добавить в очередь
        messageQueue.enqueue(message)
        showToast("Сообщение будет отправлено при восстановлении связи")
    }
}

private fun sendQueuedMessages() {
    val messages = messageQueue.dequeueAll()
    messages.forEach { message ->
        webSocketManager.send(message)
    }
}
```

---

#### **ДЕНЬ 3 (Пятница утро): WebSocket Improvements** (2-3 часа)

**Задача 3.3: Heartbeat и автопереподключение**

**Файл:** `app/src/main/java/ru/example/childwatch/network/WebSocketManager.kt`

**Что добавить:**
```kotlin
private var heartbeatJob: Job? = null
private var reconnectAttempts = 0
private val maxReconnectAttempts = 5

/**
 * Heartbeat - проверка соединения каждые 30 секунд
 */
private fun startHeartbeat() {
    heartbeatJob = lifecycleScope.launch {
        while (isActive) {
            delay(30000) // 30 секунд
            try {
                send("""{"type":"ping"}""")
                Log.d(TAG, "💓 Heartbeat sent")
            } catch (e: Exception) {
                Log.e(TAG, "Heartbeat failed, reconnecting...", e)
                reconnect()
            }
        }
    }
}

/**
 * Автоматическое переподключение с exponential backoff
 */
private fun reconnect() {
    if (reconnectAttempts >= maxReconnectAttempts) {
        Log.e(TAG, "Max reconnect attempts reached")
        showConnectionError()
        return
    }

    reconnectAttempts++
    val delay = (2.0.pow(reconnectAttempts) * 1000).toLong() // 2s, 4s, 8s, 16s, 32s

    Log.d(TAG, "Reconnecting in ${delay}ms (attempt $reconnectAttempts)")

    lifecycleScope.launch {
        delay(delay)
        connect()
    }
}

// В onOpen()
override fun onOpen(webSocket: WebSocket, response: Response) {
    Log.d(TAG, "✅ WebSocket CONNECTED")
    isConnected = true
    reconnectAttempts = 0 // сбросить счетчик
    startHeartbeat()

    // Отправить сообщения из очереди
    sendQueuedMessages()
}

// В onFailure()
override fun onFailure(webSocket: WebSocket, t: Throwable, response: Response?) {
    Log.e(TAG, "❌ WebSocket FAILED", t)
    isConnected = false
    heartbeatJob?.cancel()
    reconnect()
}
```

---

**Задача 3.4: Индикатор статуса соединения**

**Что добавить в UI:**
1. В layout чата добавить индикатор:
```xml
<TextView
    android:id="@+id/connectionStatus"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/warning_yellow"
    android:text="⚠️ Нет соединения. Сообщения будут отправлены позже"
    android:visibility="gone"
    android:padding="8dp" />
```

2. В ChatActivity обновлять статус:
```kotlin
private fun updateConnectionStatus(isConnected: Boolean) {
    binding.connectionStatus.visibility = if (isConnected) View.GONE else View.VISIBLE
}
```

---

#### **ДЕНЬ 3 (Пятница день): Тестирование надежности** (2 часа)

**Задача 3.5: Stress Testing**

**Что протестировать:**
1. **Тест 1: Плохой интернет**
   - Включить режим разработчика → ограничить скорость
   - Отправить 10 сообщений подряд
   - Проверить: все доставлены

2. **Тест 2: Переключение Wi-Fi ↔ Mobile**
   - Отправить сообщение на Wi-Fi
   - Переключиться на Mobile Data
   - Проверить: сообщение доставлено

3. **Тест 3: Режим полета**
   - Включить режим полета
   - Отправить 5 сообщений
   - Выключить режим полета
   - Проверить: все сообщения отправились из очереди

4. **Тест 4: Длительная работа**
   - Оставить приложение в фоне на 30 минут
   - Отправить сообщение
   - Проверить: доставка < 5 секунд

5. **Тест 5: Rapid Fire**
   - Отправить 50 сообщений за 10 секунд
   - Проверить: все доставлены, нет дубликатов

---

### ✅ Критерии готовности ФАЗЫ 3:
- [ ] Логирование добавлено везде
- [ ] Root cause проблемы найден
- [ ] MessageQueue реализована
- [ ] Heartbeat работает (ping каждые 30 сек)
- [ ] Автопереподключение с exponential backoff
- [ ] Индикатор статуса соединения показывается
- [ ] Offline queue: сообщения отправляются после восстановления
- [ ] Тест 1-5 пройдены успешно
- [ ] **0% потерь сообщений**
- [ ] Задержка доставки < 5 секунд (при хорошем интернете)

---

## 📅 РАСПИСАНИЕ НЕДЕЛИ (27-31 октября)

### Понедельник 27.10 (4 часа)
- ⏰ Утро: Задача 1.1 - ChatActivity ChildWatch (1.5ч)
- ⏰ День: Задача 1.2 - ChatAdapter ChildWatch (1ч)
- ⏰ Вечер: Начать Задачу 1.3 - ChatActivity ParentWatch (1.5ч)

### Вторник 28.10 (2 часа)
- ⏰ Утро: Завершить Задачу 1.3
- ⏰ День: Задача 1.4 - ChatAdapter ParentWatch (1ч)
- ⏰ Вечер: Задача 1.5 - Тестирование (30мин)
- 🎉 **РЕЛИЗ v5.6.0 / v6.4.0**

### Среда 29.10 (3 часа)
- ⏰ Утро: Задача 3.1 - Добавить логирование (2ч)
- ⏰ День: Воспроизвести проблему, собрать логи
- ⏰ Вечер: Анализ логов, определить root cause (1ч)

### Четверг 30.10 (4 часа)
- ⏰ Утро: Задача 3.2 - MessageQueue (2ч)
- ⏰ День: Интеграция MessageQueue в ChatActivity (2ч)

### Пятница 31.10 (4 часа)
- ⏰ Утро: Задача 3.3 - Heartbeat + Reconnection (2ч)
- ⏰ День: Задача 3.4 - Индикатор статуса (1ч)
- ⏰ Вечер: Задача 3.5 - Stress Testing (1ч)
- 🎉 **РЕЛИЗ v5.7.0 / v6.5.0**

**ИТОГО:** ~17 часов за неделю (среднее 3.4ч/день)

---

## 🔮 ПОСЛЕ НЕДЕЛИ 1: Следующие приоритеты

### ФАЗА 4: ИСПРАВЛЕНИЕ ГЕОЛОКАЦИИ/КАРТЫ (v5.8.0 / v6.6.0)
**Приоритет:** 🔴 КРИТИЧЕСКИЙ
**Срок:** Неделя 2 (3-7 ноября)
**Статус:** ⏳ ПОСЛЕ ФАЗЫ 3

### 📋 Проблема:
> **Из отзыва:** "геолокация еще не работает корректно (карта не отображается вообще)"

**Это тоже критично!** Карта - одна из ключевых функций мониторинга.

### Задачи (предварительный план):
1. Диагностировать почему карта не показывается
2. Проверить Google Maps API ключ
3. Проверить разрешения на геолокацию
4. Исправить баги отображения
5. Добавить историю точек на карте
6. Тестирование

---

### ФАЗА 2: МИГРАЦИЯ ЧАТА НА БД
**Приоритет:** 🟡 СРЕДНИЙ
**Срок:** Неделя 3-4
**Статус:** Отложено после геолокации

*(Менее критично, т.к. 100 сообщений пока достаточно)*

---

## ⚠️ КРИТИЧЕСКИЕ ПРОБЛЕМЫ (ПРИОРИТЕТ)

### 🔴 **КРИТИЧНО - ТРЕБУЕТ НЕМЕДЛЕННОГО ИСПРАВЛЕНИЯ:**
1. ✅ Профили детей - **РЕШЕНО** (v5.5.0)
2. ✅ QR-сканер Device ID - **РЕШЕНО** (v5.5.0)
3. ⏳ Персонализация чата - **ФАЗА 1** (эта неделя)
4. ⏳ Доставка сообщений - **ФАЗА 3** (эта неделя)
5. ⏳ Карта не работает - **ФАЗА 4** (следующая неделя)

### 🟡 **ВАЖНО - МОЖНО ОТЛОЖИТЬ:**
6. Лимит чата 100 сообщений - Фаза 2 (недели 3-4)
7. Переключение детей в меню - Фаза 5
8. Статистика по детям - Фаза 6

---

## 📊 ИСТОРИЯ ВЕРСИЙ

### v5.5.0 / v6.3.0 (26 октября 2025) - Текущая
- ✅ Room база данных
- ✅ Система профилей детей
- ✅ QR-сканер для Device ID
- ✅ Автозаполнение из контактов
- ✅ Улучшенный UI диалога добавления
- ✅ Обработка SecurityException для аватаров
- ✅ Отключение автозаполнения Device ID

### v5.6.0 / v6.4.0 (запланировано на 28 октября) - ФАЗА 1
- [ ] Персонализация чата с реальными именами
- [ ] Заголовок чата показывает имя ребенка
- [ ] ChatAdapter использует имена из БД

### v5.7.0 / v6.5.0 (запланировано на 31 октября) - ФАЗА 3
- [ ] Message Queue для офлайн сообщений
- [ ] Heartbeat для проверки соединения
- [ ] Автопереподключение WebSocket
- [ ] Индикатор статуса соединения
- [ ] 0% потерь сообщений

### v5.8.0 / v6.6.0 (запланировано на 7 ноября) - ФАЗА 4
- [ ] Исправление отображения карты
- [ ] История точек геолокации
- [ ] Улучшения UI карты

---

## 📝 ИНСТРУКЦИЯ ДЛЯ ПРОДОЛЖЕНИЯ РАБОТЫ

Если разработку продолжает другой человек:

1. **Прочитайте этот документ полностью**
2. **Проверьте текущий статус:**
   - Какая фаза сейчас в работе?
   - Какие задачи выполнены? (отмечены ✅)
3. **Найдите следующую невыполненную задачу**
4. **Следуйте детальному плану для этой задачи**
5. **После выполнения:**
   - Отметьте задачу как выполненную (✅)
   - Протестируйте
   - Сделайте commit
   - Переходите к следующей задаче
6. **Обновите этот документ:**
   - Отметьте прогресс
   - Добавьте примечания если что-то изменилось

---

## 🎯 МЕТРИКИ УСПЕХА

### Неделя 1 (27-31 октября) - УСПЕХ если:
- [x] Профили детей работают ✅
- [x] QR-сканер работает ✅
- [ ] ✅ Персонализация чата работает (ФАЗА 1)
- [ ] ✅ Сообщения НЕ теряются (ФАЗА 3)
- [ ] ✅ Heartbeat работает
- [ ] ✅ Message Queue работает
- [ ] ✅ Stress тесты пройдены

### Неделя 2 (3-7 ноября) - УСПЕХ если:
- [ ] ✅ Карта отображается
- [ ] ✅ История геолокации работает
- [ ] ✅ Все основные функции работают стабильно

---

**Последнее обновление:** 26 октября 2025, 19:00
**Утверждено:** dr-ed + Claude
**Следующий пересмотр:** 28 октября (после ФАЗЫ 1)
**Статус:** 📋 ПЛАН УТВЕРЖДЕН И ГОТОВ К ВЫПОЛНЕНИЮ
