# ChildWatch v4.4.1 - Исправления багов

## Дата: 26 октября 2025

---

## 🐛 Исправленные баги:

### 1. ✅ Аватарка не обновляется на главной странице

**Проблема**: После изменения аватарки ребенка в профиле, на главной странице продолжала отображаться старая аватарка.

**Причина**: Методы `loadSelectedChild()` и `updateSelectedChild()` в MainActivity не загружали и не обновляли аватар из базы данных.

**Решение**:
- Добавлена загрузка `child.avatarUrl` в `loadSelectedChild()`
- Добавлена загрузка `child.avatarUrl` в `updateSelectedChild()`
- Добавлен сброс аватара в `showDefaultChildSelection()`
- Обработка ошибок с fallback на иконку по умолчанию

**Файлы**:
- `app/src/main/java/ru/example/childwatch/MainActivity.kt`

**Код**:
```kotlin
// Загрузить аватар
if (child.avatarUrl != null) {
    try {
        binding.selectedChildAvatar.setImageURI(android.net.Uri.parse(child.avatarUrl))
    } catch (e: Exception) {
        Log.e(TAG, "Error loading avatar", e)
        binding.selectedChildAvatar.setImageResource(android.R.drawable.ic_menu_myplaces)
    }
} else {
    binding.selectedChildAvatar.setImageResource(android.R.drawable.ic_menu_myplaces)
}
```

---

### 2. ✅ Краш при создании второго ребенка с аватаром

**Проблема**: Приложение вылетало после создания второго профиля ребенка.

**Возможная причина**: Android мог удалять временный доступ к URI файла изображения, что приводило к ошибкам при попытке доступа к аватару.

**Решение**:
- Добавлен запрос на **постоянный доступ к URI** (`takePersistableUriPermission`)
- Используется флаг `FLAG_GRANT_READ_URI_PERMISSION`
- Добавлена обработка ошибок с fallback на временный URI

**Файлы**:
- `app/src/main/java/ru/example/childwatch/ChildSelectionActivity.kt`

**Код**:
```kotlin
private val pickImageLauncher = registerForActivityResult(
    ActivityResultContracts.GetContent()
) { uri ->
    uri?.let {
        try {
            // Запросить постоянный доступ к URI
            val takeFlags = Intent.FLAG_GRANT_READ_URI_PERMISSION
            contentResolver.takePersistableUriPermission(uri, takeFlags)

            selectedAvatarUri = it
            currentAvatarImageView?.setImageURI(it)
            Log.d(TAG, "Avatar selected with persistent permission: $it")
        } catch (e: Exception) {
            // Если не получилось получить постоянный доступ, все равно используем URI
            Log.w(TAG, "Could not take persistable URI permission: ${e.message}")
            selectedAvatarUri = it
            currentAvatarImageView?.setImageURI(it)
        }
    }
}
```

---

## 🔍 Диагностика: Доставка сообщений

### Возможная проблема

Вы сообщили, что доставка сообщений не работает. Это может быть связано с:

1. **WebSocket не подключен** к серверу
2. **Мониторинг не запущен** (службы LocationService, ChatBackgroundService)
3. **Нет разрешений** на фоновую работу или уведомления
4. **Сервер недоступен** (VPS на 31.28.27.96:3000)

### Как проверить:

#### 1. Проверка подключения к серверу

На главной странице ChildWatch смотрите на карточку **"Monitoring Status"**:
- Должен быть **зеленый статус** "✅ Мониторинг активен"
- Статус локации, аудио, фото должны быть активны

#### 2. Запуск мониторинга

На главной странице:
1. Нажмите кнопку **"Начать мониторинг"**
2. Разрешите все запрашиваемые разрешения:
   - Доступ к местоположению (всегда)
   - Доступ к микрофону
   - Доступ к камере
   - Уведомления (Android 13+)
3. Отключите оптимизацию батареи для ChildWatch

#### 3. Проверка выбранного устройства

На главной странице в карточке **"Child Device Selection"**:
- Должно быть выбрано конкретное устройство ребенка
- Показывается имя и Device ID
- Если показано "Выберите устройство" - нажмите на карточку и выберите ребенка

#### 4. Проверка чата

Откройте чат:
1. Нажмите на карточку **"Чат"** в главном меню
2. В заголовке должно быть "Чат с [имя ребенка]"
3. Отправьте тестовое сообщение
4. Проверьте логи через adb:

```bash
adb logcat | grep -E "MessageQueue|WebSocket|ChatActivity"
```

**Ожидаемые логи**:
```
MessageQueue: Message queued: тест, pending: 1
MessageQueue: Processing queue, size: 1
WebSocketManager: Message sent successfully
MessageQueue: Message XXXXX sent successfully
MessageQueue: Queue processing completed, 0 messages remaining
```

**Если есть ошибки**:
```
WebSocketManager: WebSocket not connected
MessageQueue: Message XXXXX failed (attempt 1/10): WebSocket not connected
```

#### 5. Если сообщения не доставляются:

**Решение А: Перезапустить мониторинг**
1. Нажмите **"Остановить мониторинг"**
2. Подождите 5 секунд
3. Нажмите **"Начать мониторинг"**
4. Проверьте статус подключения

**Решение Б: Очистить данные и перезапустить**
1. Settings → Apps → ChildWatch → Storage → Clear Cache
2. Перезапустите приложение
3. Заново включите мониторинг

**Решение В: Проверить сервер**
1. Откройте браузер на телефоне
2. Перейдите на `http://31.28.27.96:3000/health`
3. Должен отобразиться `{"status":"ok"}`
4. Если сервер недоступен - сообщите для перезапуска

#### 6. Логи MessageQueue

MessageQueue автоматически:
- Сохраняет сообщения при отправке
- Повторяет попытки каждые 5 секунд
- Делает до 10 попыток
- Сохраняет очередь при закрытии приложения

**Проверка очереди через логи**:
```bash
adb logcat | grep MessageQueue
```

Вы увидите:
- `Message queued:` - сообщение добавлено в очередь
- `Processing queue, size: X` - начало обработки очереди
- `Message XXXX sent successfully` - успешная отправка
- `Message XXXX failed (attempt X/10)` - неудачная попытка
- `Queue processing completed, X messages remaining` - завершение обработки

---

## 📱 Обновленный APK

После исправлений:

```bash
./gradlew.bat :app:assembleDebug
```

APK файл: `app/build/outputs/apk/debug/ChildWatch-v5.5.0-debug.apk`

---

## 🧪 Тестирование

### Тест 1: Обновление аватарки

1. Откройте главную страницу ChildWatch
2. Нажмите на карточку выбора устройства
3. В списке нажмите на иконку редактирования (✏️) рядом с ребенком
4. Нажмите "Выбрать фото" и выберите новое изображение
5. Нажмите "Сохранить"
6. **Ожидаемый результат**: Аватарка обновилась на главной странице сразу после возврата

### Тест 2: Создание нескольких детей с аватарами

1. Откройте список устройств (нажмите на карточку выбора устройства)
2. Нажмите FAB кнопку "+" для добавления
3. Введите Device ID и имя первого ребенка
4. Выберите фото для аватара
5. Нажмите "Добавить"
6. Повторите шаги 2-5 для второго ребенка
7. **Ожидаемый результат**: Оба профиля созданы, аватарки отображаются в списке, приложение не крашится

### Тест 3: Доставка сообщений (после включения мониторинга)

1. Запустите мониторинг на ChildWatch
2. Откройте ParentWatch на устройстве ребенка
3. Запустите мониторинг на ParentWatch
4. Откройте чат на ChildWatch
5. Отправьте сообщение
6. **Ожидаемый результат**: Сообщение доставлено на ParentWatch в течение 1-2 секунд
7. Отключите интернет на ChildWatch
8. Отправьте еще одно сообщение
9. **Ожидаемый результат**: Сообщение добавлено в очередь (проверить логи)
10. Включите интернет
11. **Ожидаемый результат**: Сообщение автоматически доставлено в течение 5 секунд

---

## 📝 Коммит

```
e74b2b0 - fix(childwatch): avatar display and URI persistence issues
```

---

## 🔜 Следующие шаги

Если проблемы с доставкой сообщений сохраняются:

1. **Предоставьте логи**:
   ```bash
   adb logcat -d > childwatch_logs.txt
   ```
   И отправьте файл `childwatch_logs.txt`

2. **Опишите симптомы**:
   - Мониторинг запущен? (зеленый статус на главной)
   - WebSocket подключен? (проверьте логи)
   - Сообщения в очереди? (размер очереди в логах)
   - Ошибки при отправке? (тексты ошибок)

3. **Проверьте сервер**:
   - Доступен ли `http://31.28.27.96:3000`?
   - Работает ли WebSocket `ws://31.28.27.96:3000`?

Я готов помочь с дальнейшей диагностикой! 🚀
