# Changelog v5.5.0 - Улучшения управления устройствами

**Дата**: 25 октября 2025

## ✨ Новые возможности

### Редактирование данных устройств

- ✅ Добавлена кнопка редактирования на каждой карточке устройства
- ✅ Возможность изменить имя устройства после добавления
- ✅ Долгое нажатие на карточку как альтернативный способ открыть редактирование
- ✅ Удобный диалог редактирования с тремя кнопками: Сохранить, Отмена, Удалить

### Удаление устройств

- ✅ Возможность удалить устройство из списка
- ✅ Диалог подтверждения удаления для предотвращения случайного удаления
- ✅ Автоматический сброс выбора при удалении активного устройства

### Улучшения интерфейса

- ✅ Визуальная кнопка редактирования (иконка карандаша) на каждой карточке
- ✅ Улучшенное расположение элементов на карточке устройства
- ✅ Device ID теперь нельзя изменить (защита от ошибок)

## 🐛 Исправления ошибок

### Критические исправления

- 🔧 **ИСПРАВЛЕНО**: Краш при открытии ChildSelectionActivity из-за конфликта ActionBar
  - Создана специальная тема `Theme.ChildWatch.NoActionBar`
  - Активность теперь открывается без ошибок
- 🔧 **ИСПРАВЛЕНО**: Кнопка "Выберите устройство" не работала
  - Убран конфликтующий `foreground` атрибут с MaterialCardView
  - Упрощена логика обработчиков кликов
  - Добавлен `background` с `selectableItemBackground` на контейнер

### Улучшения стабильности

- 🔧 Улучшена обработка ошибок при работе с базой данных
- 🔧 Добавлено больше логирования для диагностики проблем
- 🔧 Оптимизирована загрузка списка устройств

## 📋 Технические детали

### Изменённые файлы

- `item_child.xml` - добавлена кнопка редактирования
- `ChildrenAdapter.kt` - добавлен callback для редактирования и долгое нажатие
- `ChildSelectionActivity.kt` - добавлены функции редактирования и удаления
- `activity_main_menu.xml` - исправлен layout карточки выбора устройства
- `MainActivity.kt` - упрощены обработчики кликов
- `themes.xml` - добавлена тема NoActionBar
- `AndroidManifest.xml` - применена NoActionBar тема к ChildSelectionActivity

### Новые методы

- `showEditChildDialog(child: Child)` - диалог редактирования
- `updateChild(child: Child, newName: String)` - обновление данных
- `showDeleteConfirmDialog(child: Child)` - подтверждение удаления
- `deleteChild(child: Child)` - удаление устройства

### API изменения

- `ChildrenAdapter` теперь принимает два callback: `onChildClick` и `onChildEdit`
- Добавлена поддержка долгого нажатия на карточки устройств

## 📱 Системные требования

- Android 8.0 (API 26) и выше
- Минимум 50 МБ свободного места

## 🔄 Обновление

1. Удалите старую версию или установите поверх существующей
2. Все данные (устройства, настройки) сохранятся
3. При первом запуске проверьте список устройств

## 📝 Известные ограничения

- Device ID нельзя изменить после создания (это сделано намеренно для безопасности)
- Удаление устройства необратимо
- История данных устройства сохраняется в БД даже после удаления

## 👨‍💻 Для разработчиков

### Добавление новых полей редактирования

Чтобы добавить новые редактируемые поля:

1. Обновите `dialog_add_child.xml`
2. Добавьте поля в `Child` entity
3. Обновите методы `showEditChildDialog()` и `updateChild()`

### Тестирование

```bash
# Сборка
.\gradlew.bat assembleDebug

# Установка
adb install -r app/build/outputs/apk/debug/ChildWatch-v5.5.0-debug.apk

# Запуск
adb shell monkey -p ru.example.childwatch -c android.intent.category.LAUNCHER 1
```

## 🙏 Благодарности

Спасибо за использование ChildWatch! Если у вас есть предложения или вы нашли ошибку, создайте issue на GitHub.

---

**Полное руководство**: См. `CHILD_MANAGEMENT_GUIDE.md`
