# План реализации v7.0.0 - Подробный

> **Текущий прогресс:** История перемещений ✅ | Удалённое фото 60% | Чат 40%

## 🎯 Рекомендуемый порядок выполнения

### Фаза 1: Завершить удалённое фото (P0) — **СЕЙЧАС**
**Время:** 2-3 часа | **Приоритет:** 🔥 КРИТИЧНО

#### Задача 1.1: Интеграция RemotePhotoService с WebSocket (ParentWatch)
**Файлы:** `parentwatch/src/main/java/ru/example/parentwatch/network/WebSocketManager.kt`

**Шаги:**
1. Добавить BroadcastReceiver для прослушивания `PHOTO_CAPTURED`
2. В обработчике события `request_photo`:
   - Запустить RemotePhotoService через Intent
   - Передать requestId и from_parent
3. При получении broadcast `PHOTO_CAPTURED`:
   - Конвертировать ByteArray в Base64 (или использовать binary)
   - Отправить через `socket.emit("photo", data)`
4. Обработать broadcast `PHOTO_ERROR` и отправить на сервер

**Код-каркас:**
```kotlin
// В WebSocketManager.kt
private val photoCaptureReceiver = object : BroadcastReceiver() {
    override fun onReceive(context: Context, intent: Intent) {
        when (intent.action) {
            "ru.example.parentwatch.PHOTO_CAPTURED" -> {
                val requestId = intent.getStringExtra("request_id")
                val imageData = intent.getByteArrayExtra("image_data")
                // ... отправка через socket
            }
        }
    }
}
```

**Проверка:** Логи показывают отправку фото на сервер

---

#### Задача 1.2: UI запроса фото (ChildWatch)
**Файлы:** 
- `app/src/main/java/ru/example/childwatch/MainActivity.kt` (или ChildSelectionActivity)
- `app/src/main/java/ru/example/childwatch/PhotoPreviewActivity.kt` (новый)
- `app/src/main/res/layout/activity_photo_preview.xml` (новый)

**Шаги:**
1. Добавить FloatingActionButton 📷 в главное меню рядом с кнопкой мониторинга
2. По клику:
   - Проверить, что устройство выбрано
   - Показать ProgressDialog "Запрос отправлен..."
   - Отправить через WebSocket: `socket.emit("request_photo", { targetDevice: childId, requestId: UUID })`
3. Обработать событие `photo`:
   - Закрыть ProgressDialog
   - Декодировать Base64 в Bitmap
   - Открыть PhotoPreviewActivity с изображением
4. Обработать событие `photo_error`:
   - Показать Toast с ошибкой

**PhotoPreviewActivity:**
- ImageView для отображения
- Кнопки: "Сохранить", "Поделиться", "Закрыть"
- Metadata: дата, время, deviceId

**Проверка:** Нажатие кнопки → получение фото → отображение

---

#### Задача 1.3: Notification при получении фото
**Файл:** `app/src/main/java/ru/example/childwatch/network/WebSocketManager.kt`

**Шаги:**
1. При получении события `photo` в фоне:
   - Создать NotificationChannel "remote_photos"
   - Использовать BigPictureStyle
   - Intent для открытия PhotoPreviewActivity
2. Сохранить фото локально:
   - `context.getExternalFilesDir(Environment.DIRECTORY_PICTURES)/RemotePhotos/`
   - Имя файла: `photo_${deviceId}_${timestamp}.jpg`

**Проверка:** Фото приходит в фоне → notification → открывается при нажатии

---

#### Задача 1.4: Обработка ошибок и таймаутов
**Файлы:** Обновление WebSocketManager в обоих приложениях

**Шаги:**
1. На клиенте (ChildWatch):
   - Таймер 30 секунд после отправки запроса
   - Если не пришло `photo` → показать "Таймаут запроса"
2. Обработать все типы ошибок:
   - "Device offline" → "Устройство недоступно"
   - "Camera busy" → "Камера занята"
   - "Permission denied" → "Нет разрешения камеры"
3. Добавить индикацию состояния:
   - Кнопка 📷 становится неактивной во время запроса
   - Spinner вместо иконки

**Проверка:** Все сценарии ошибок обработаны корректно

---

#### Задача 1.5: End-to-End тестирование
**Сценарии:**
1. ✅ Успешный захват: родитель → ребёнок онлайн → фото приходит
2. ✅ Офлайн устройство: родитель → ребёнок офлайн → ошибка "недоступно"
3. ✅ Фоновый режим: приложение ребёнка свёрнуто → фото захватывается
4. ✅ Таймаут: ребёнок не отвечает → ошибка через 30 сек
5. ✅ Качество: фото сжато до ~500KB, разрешение 1280x720

**Чеклист:**
- [ ] Сборка обоих APK без ошибок
- [ ] Установка на реальные устройства
- [ ] Все 5 сценариев пройдены
- [ ] Логи чистые (нет критичных ошибок)

---

### Фаза 2: Устойчивость чата (P0)
**Время:** 1-2 часа | **Приоритет:** ⚡ ВЫСОКИЙ

#### Задача 2.1: Автопереподключение WebSocket
**Файл:** `app/src/main/java/ru/example/childwatch/network/WebSocketManager.kt` (и ParentWatch)

**Шаги:**
1. Добавить переменные:
```kotlin
private var reconnectAttempts = 0
private var reconnectDelayMs = 1000L
private val maxReconnectDelay = 30000L
```

2. При `disconnect`:
   - Не сбрасывать сразу
   - Запустить экспоненциальный backoff
   - `delay = min(1000 * 2^attempts, 30000)`

3. При `connect`:
   - Сбросить reconnectAttempts = 0

**Проверка:** Выключить WiFi → включить → чат восстанавливается автоматически

---

#### Задача 2.2: Ping/Pong Heartbeat
**Файл:** Серверный `WebSocketManager.js` + клиенты

**Шаги:**
1. Клиент отправляет `ping` каждые 30 секунд
2. Сервер отвечает `pong` с timestamp
3. Если нет `pong` 60 секунд → разрыв соединения → переподключение

**Проверка:** Логи показывают регулярные ping/pong

---

#### Задача 2.3: Индикатор статуса соединения
**Файл:** `app/src/main/res/layout/activity_chat.xml` + ChatActivity

**Шаги:**
1. Добавить TextView в toolbar: "Подключение..." / "Онлайн"
2. Обновлять при событиях connect/disconnect
3. Цвета: серый (подключение), зелёный (онлайн), красный (офлайн)

**Проверка:** Визуально видно состояние соединения

---

#### Задача 2.4: Очередь неотправленных сообщений
**Файл:** ChatActivity + локальная БД

**Шаги:**
1. При отправке сообщения:
   - Сохранить в локальную БД с флагом `pending=true`
   - Попытаться отправить через WebSocket
2. Если офлайн:
   - Показать индикатор "Отправка..."
3. При восстановлении соединения:
   - Отправить все pending сообщения
   - Обновить UI после успешной отправки

**Проверка:** Офлайн → отправка сообщения → онлайн → сообщение доставлено

---

### Фаза 3: Геозоны (P1) — ПОСЛЕ P0
**Время:** 3-4 часа | **Приоритет:** 🎯 СРЕДНИЙ

*(Подробный план можно развернуть позже)*

---

### Фаза 4: Финализация v7.0.0
**Время:** 1 час | **Приоритет:** 📦 ПЕРЕД РЕЛИЗОМ

#### Задача 4.1: Обновление версий
```gradle
// app/build.gradle
versionCode 41  // было 40
versionName "7.0.0"

// parentwatch/build.gradle
versionCode 28  // было 27
versionName "7.0.0"
```

#### Задача 4.2: CHANGELOG.md
```markdown
## [7.0.0] - 2025-11-XX

### Добавлено
- 📍 История перемещений с выбором периода
- 📷 Удалённый захват фото с устройства ребёнка
- 💬 Устойчивое WebSocket соединение чата
- 🔔 Уведомления для удалённых фото

### Улучшено
- Карта теперь показывает траекторию движения
- Чат автоматически переподключается при разрыве
- Повышена надёжность мониторинга
```

#### Задача 4.3: Скриншоты
- [ ] История перемещений (траектория на карте)
- [ ] Удалённое фото (preview + notification)
- [ ] Чат (индикатор соединения)

#### Задача 4.4: Финальное тестирование
- [ ] Регресс: все старые функции работают
- [ ] Новые функции: история, фото, чат
- [ ] Обе платформы: Nokia (ParentWatch) + Pixel 8 (ChildWatch)

---

## 📊 Метрики прогресса

### Текущее состояние (6 ноября)
- ✅ История перемещений: **100%**
- 🔄 Удалённое фото: **60%** (осталось: UI родителя, notification, тесты)
- 🔄 Устойчивость чата: **20%** (осталось: автопереподключение, heartbeat, очередь)
- ⏸️ Геозоны: **0%** (запланировано)

### Оценка времени до релиза
- **Фаза 1 (удалённое фото):** 2-3 часа
- **Фаза 2 (устойчивость чата):** 1-2 часа
- **Фаза 3 (геозоны):** 3-4 часа (опционально для v7.0)
- **Фаза 4 (финализация):** 1 час

**Итого минимум (без геозон):** 4-6 часов → релиз возможен сегодня/завтра
**Итого полный v7.0 (с геозонами):** 7-10 часов → релиз через 2 дня

---

## 🎯 Рекомендация

### Вариант А: Быстрый релиз v7.0.0-beta (сегодня)
**Включает:**
- ✅ История перемещений
- ✅ Удалённое фото (базовое)
- ✅ Устойчивость чата

**Не включает:**
- ❌ Геозоны → отложить на v7.1.0

**Плюсы:**
- Быстрый выход новых функций
- Можно протестировать в реальных условиях
- Фидбек от пользователей раньше

---

### Вариант Б: Полный v7.0.0 (через 2 дня)
**Включает всё** по плану включая геозоны

**Плюсы:**
- Полноценный релиз согласно roadmap
- Все P0 + P1 функции

**Минусы:**
- Дольше до релиза
- Больше рисков одновременно

---

## 🚀 Что делать СЕЙЧАС?

Я рекомендую **Вариант А** — завершить удалённое фото и чат, выпустить v7.0.0-beta.

**Следующий шаг:** Начать с **Задачи 1.1** (интеграция RemotePhotoService с WebSocket).

Готов приступить? Или хочешь обсудить/изменить план?
