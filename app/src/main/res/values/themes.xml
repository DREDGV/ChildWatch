<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.ChildWatch" parent="Theme.ChildWatch.DesignSystem" />

    <style name="Theme.ChildWatch" parent="Base.Theme.ChildWatch" />

    <!-- Theme for activities with custom Toolbar (no ActionBar) -->
    <style name="Theme.ChildWatch.NoActionBar" parent="Base.Theme.ChildWatch">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>
